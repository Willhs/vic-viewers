var screenWidth = $(window).width() * 0.8,
    screenHeight = $(window).height();

var svg = d3.select("body").append("svg")
            .attr("width", screenWith)
            .attr('height', screenHeight);


